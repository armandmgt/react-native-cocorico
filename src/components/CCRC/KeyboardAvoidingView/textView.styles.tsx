import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  shrinkable: {
    flex: 1,
    flexDirection: 'row',
    alignContent: 'flex-end',
    alignItems: 'center',
    flexWrap: 'wrap',
  },
});
